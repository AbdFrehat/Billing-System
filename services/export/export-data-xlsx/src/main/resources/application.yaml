spring:
  webflux:
    base-path: /selling/export/sales/xlsx/v1/
  application:
    name: export-data-xlsx
server:
  port: 7032

springdoc:
  swagger-ui:
    path: /swagger-ui
  api-docs:
    groups:
      enabled: true
    path: /api/docs
config:
  services:
    context-path:
      data-manager-ms: "http://DATA-MANAGER/selling/data/manager/sale/v1/"

