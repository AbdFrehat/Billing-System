spring:
  webflux:
    base-path: /orderizer/export/data/json/v1/
  application:
    name: export-data-json
server:
  port: 7033
springdoc:
  swagger-ui:
    path: /swagger-ui
  api-docs:
    groups:
      enabled: true
    path: /api/docs
config:
  services:
    context-path:
      data-manager: "http://localhost:6080/orderizer/data/manager/v1/"
  pagination:
    size: 100
