spring:
  webflux:
    base-path: /selling/query/get/free/sale/v1/
  data:
    mongodb:
      uri: mongodb://localhost:27017/sample_supplies
  application:
    name: sales-free-get-ms
  autoconfigure:
    exclude:
server:
  port: 6023
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8003/selling/query/eureka/
springdoc:
  swagger-ui:
    path: /swagger-ui
  api-docs:
    groups:
      enabled: true
    path: /api/docs
logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: ERROR
config:
  query:
    builder: query-free-builder