server:
  port: 8000
  servlet:
    context-path: /selling/fetch/
spring:
  application:
    name: fetch-eureka-server
eureka:
  client:
    registerWithEureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8000/selling/fetch/eureka/