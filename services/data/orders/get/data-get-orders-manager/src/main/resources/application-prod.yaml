spring:

server:
  port: ${PORT}
config:
  services:
    context-path:
      data-get-free-orders: ${DATA_GET_FREE_ORDERS_URL}
      data-get-search-orders: ${DATA_GET_SEARCH_ORDERS_URL}
      data-get-operation-orders: ${DATA_GET_OPERATION_ORDERS_URL}
  logging:
    enable: ${ENABLE_LOGGING_FILES}