server:
  port: ${PORT}
config:
  services:
    context-path:
      data-delete-order: ${DATA_DELETE_ORDER_URL}
      data-delete-orders: ${DATA_DELETE_ORDERS_URL}
      data-delete-search-orders: ${DATA_DELETE_SEARCH_ORDERS_URL}
  logging:
    enable: ${ENABLE_LOGGING_FILES}
