server:
  port: ${PORT}
config:
  services:
    context-path:
      data-delete-ms: ${DATA_SALES_DELETE_QUERY_URL}
      data-delete-multi-ms: ${DATA_SALES_DELETE_URL}
      data-delete-query-ms: ${DATA_SALES_DELETE_QUERY_URL}
  logging:
    enable: ${ENABLE_LOGGING_FILES}
