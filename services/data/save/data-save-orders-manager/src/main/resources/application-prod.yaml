spring:

server:
  port: ${PORT}
config:
  services:
    context-path:
      data-save-order: ${DATA_SAVE_ORDER_URL}
      data-save-orders: ${DATA_SAVE_ORDERS_URL}
  logging:
    enable: ${ENABLE_LOGGING_FILES}