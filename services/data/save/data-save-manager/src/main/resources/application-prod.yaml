spring:

server:
  port: ${PORT}
config:
  services:
    context-path:
      data-save-ms: ${DATA_SALE_SAVE_URL}
      data-save-multi-ms: ${DATA_SALES_SAVE_URL}
  logging:
    enable: ${ENABLE_LOGGING_FILES}