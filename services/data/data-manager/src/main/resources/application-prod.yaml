server:
  port: ${PORT}
config:
  services:
    context-path:
      data-get-orders-manager: ${DATA_GET_ORDERS_MANAGER_URL}
      data-save-orders-manager: ${DATA_SAVE_ORDERS_MANAGER_URL}
      data-update-orders-manager: ${DATA_UPDATE_ORDERS_MANAGER_URL}
      data-delete-orders-manager: ${DATA_DELETE_ORDERS_MANAGER_URL}
  logging:
    enable: ${ENABLE_LOGGING_FILES}
