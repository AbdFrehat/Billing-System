spring:

server:
  port: ${PORT}
config:
  services:
    context-path:
      data-update-ms: ${DATA_SALE_UPDATE_URL}
      data-update-multi-ms: ${DATA_SALES_UPDATE_URL}
  logging:
    enable: ${ENABLE_LOGGING_FILES}
