spring:

server:
  port: ${PORT}
config:
  services:
    context-path:
      data-update-order: ${DATA_UPDATE_ORDER_URL}
      data-update-orders: ${DATA_UPDATE_ORDERS_URL}
  logging:
    enable: ${ENABLE_LOGGING_FILES}
