spring:

server:
  port: ${PORT}
config:
  services:
    context-path:
      data-get-free-ms: ${DATA_SALES_GET_FREE_URL}
      data-get-ms: ${DATA_SALES_GET_URL}
      data-get-opt-ms: ${DATA_SALES_GET_OPT_URL}
  logging:
    enable: ${ENABLE_LOGGING_FILES}